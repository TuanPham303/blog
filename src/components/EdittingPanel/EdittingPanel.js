import React from 'react';
import { EdittingIcon } from '..';

const EdittingPanel = () => (
  <div>
    <EdittingIcon />
    <EdittingIcon />
    <EdittingIcon />
    <EdittingIcon />
  </div>
);

export default EdittingPanel;
