import React from 'react';
import { Buttons } from 'components';

const EdittingPanel = () => (
  <div>
    <Buttons />
    <Buttons />
    <Buttons />
    <Buttons />
  </div>
);

export default EdittingPanel;
